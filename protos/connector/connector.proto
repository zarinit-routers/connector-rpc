syntax = "proto3";

option go_package = "github.com/zarinit-routers/router-server/connector";

message Node {
    string id = 1;
    string name = 2;
    repeated string tags = 3;
}

message NodesByGroupRequest {
    string groupId = 1;
}

message NodesResponse {
    repeated Node clients = 1;
}
message TagRequest {
    string modeId = 1;
    string tag = 2;
}

service Nodes {
    rpc NodesByGroup(NodesByGroupRequest) returns (NodesResponse) {}
    rpc AddTag(TagRequest) returns (Node) {}
    rpc RemoveTag(TagRequest) returns (Node) {}
}
